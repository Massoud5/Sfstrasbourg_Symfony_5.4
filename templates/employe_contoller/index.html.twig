{% extends 'base.html.twig' %}

{% block title %}Hello EmployeContollerController!{% endblock %}

{% block body %}

    <h1>List des employés</h1>

    <a class="btn AjouterBtn" href="{{ path('add_employe') }}">Ajouter un employe</a> <br>
        <div class="liste">
    {# afficher la liste des employes #}
        {% for employe in employes %}
            <br>
            <a href="{{ path('show_employe', {'id': employe.id}) }}">{{ employe }}</a> ({{ employe.entreprise }})
            <a class="btn editBtn" href="{{ path('edit_employe', {'id': employe.id}) }}">Edit</a>
            <a class="btn deleteBtn" href="{{ path('delete_employe', {'id': employe.id}) }}">Delete</a><br>
            
        {% endfor %}
        </div>

{% endblock %}
